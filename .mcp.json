{
  "mcpServers": {
    "system-task": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/stdio.js"],
      "env": {
        "SYSTEM_TASK_TOKEN": "${user_config.token}",
        "SYSTEM_TASK_API_URL": "${user_config.apiUrl}"
      }
    }
  }
}
